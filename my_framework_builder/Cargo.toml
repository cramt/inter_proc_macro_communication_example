[package]
name = "my_framework_builder"
version = "0.1.0"
edition = "2024"

[dependencies]
plugin_api = { path = "../plugin_api" }
rapace = { version = "0.4.0", features = ["stream"] }
tokio = { version = "1", features = ["full"] }
clap = { version = "4", features = ["derive"] }
