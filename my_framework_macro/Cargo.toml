[package]
name = "my_framework_macro"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
rapace = { version = "0.4.0", features = ["stream"] }
tokio = { version = "1", features = ["full"] }
tempfile = "3"
plugin_api = { path = "../plugin_api" }
